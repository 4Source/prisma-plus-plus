#include "core/Subray.h"
void Subray::setDirection(glm::vec3 dir) { direction_ = dir; }
void Subray::setPosition(glm::vec3 pos) { postition_ = pos; }
void Subray::setColor(glm::vec3 col) { color_ = col; }